# squirrelspotter-server

FastAPI backend for SquirrelSpotter. See the [root README](../README.md) for setup.

## Automatic API documentation

The server exposes a single interactive API docs page through FastAPI:

- Swagger UI: http://localhost:8080/docs

This docs page is generated automatically from the route declarations and docstrings, so it stays in sync as the API evolves.
